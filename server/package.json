{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
  "build": "tsc --target ES2022 --module NodeNext --moduleResolution NodeNext --rootDir ./src --outDir ./dist --strict false --skipLibCheck true",
  "start": "node dist/index.js"
},
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}